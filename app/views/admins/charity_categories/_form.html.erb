<%= form_for(@charity_category, :url => url) do |f| %>
    <% if @charity_category.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@charity_category.errors.count, "error") %> prohibited this charity category from being
            saved:</h2>

          <ul>
            <% @charity_category.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="field">
      <%= f.label :name %>
      <%= f.text_field :name %>
    </div>

    <div class="actions">
      <%= f.submit "Save", :class => "smallBtn" %>
    </div>
<% end %>
