<%= form_for(@size_category, :url => url ) do |f| %>
    <% if @size_category.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@size_category.errors.count, "error") %> prohibited this shirt size from being
            saved:</h2>

          <ul>
            <% @size_category.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="field">
      <%= f.label :name %>
      <%= f.text_field :name %>
    </div>
    <div class="actions">
      <%= f.submit "Save", :class => "smallBtn" %>
    </div>
<% end %>
